name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - "feat/**"
      - "fix/**"
      - "chore/**"
      - "docs/**"

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  php:
    name: PHP ${{ matrix.php }} • Bootstrap only
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['8.3', '8.4']
    env:
      APP_ENV: test
      DATABASE_URL: "sqlite:///%kernel.project_dir%/var/data_%kernel.environment%.db"
      ENABLE_NATIVE_LAZY_OBJECTS: "0"
      SYMFONY_DEPRECATIONS_HELPER: disabled

    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: composer
          extensions: pdo_sqlite, sqlite3, mbstring, intl, xml, zip, curl

      # Hard-lock per PHP version BEFORE any Symfony command
      - name: Force native lazy OFF on PHP 8.3
        if: matrix.php == '8.3'
        run: echo "ENABLE_NATIVE_LAZY_OBJECTS=0" >> "$GITHUB_ENV"

      - name: Force native lazy ON on PHP 8.4
        if: matrix.php == '8.4'
        run: echo "ENABLE_NATIVE_LAZY_OBJECTS=1" >> "$GITHUB_ENV"

      - name: Show versions & env
        run: |
          php -v
          composer -V
          echo "APP_ENV=$APP_ENV"
          echo "DATABASE_URL=$DATABASE_URL"
          echo "ENABLE_NATIVE_LAZY_OBJECTS=$ENABLE_NATIVE_LAZY_OBJECTS"
          php -m | grep -Ei 'pdo|sqlite' || true

      - name: Restore Composer cache (optional)
        if: hashFiles('composer.lock') != ''
        uses: actions/cache@v4
        with:
          path: ~/.composer/cache/files
          key: composer-${{ runner.os }}-${{ matrix.php }}-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            composer-${{ runner.os }}-${{ matrix.php }}-
            composer-${{ runner.os }}-

      # Skip auto-scripts so cache:clear is not invoked during install
      - name: Install dependencies (optional)
        if: hashFiles('composer.json') != ''
        env:
          SYMFONY_SKIP_AUTO_SCRIPTS: "1"
        run: composer install --prefer-dist --no-interaction --no-progress --no-scripts

      - name: Composer validate (optional)
        if: hashFiles('composer.json') != ''
        run: composer validate --strict --no-check-publish

      # Preflight on test env
      - name: Symfony about (preflight)
        run: php bin/console about --env=test

      - name: DB ping (preflight)
        run: php bin/console dbal:run-sql "SELECT 1" --env=test

      # Optional Composer scripts
      - id: detect-scripts
        if: hashFiles('composer.json') != ''
        run: |
          php -r '$j=@json_decode(@file_get_contents("composer.json"),true)?:[];
          foreach (["cs:check","stan","test"] as $k) {
            $v = (isset($j["scripts"][$k]) ? "1" : "0");
            $out = strtoupper(str_replace([":","-"],["_","_"],$k));
            echo "$out=$v\n";
          }' >> $GITHUB_OUTPUT

      - name: Code style check (optional)
        if: ${{ steps.detect-scripts.outputs.CS_CHECK == '1' }}
        run: composer cs:check

      - name: Static analysis (optional)
        if: ${{ steps.detect-scripts.outputs.STAN == '1' }}
        run: composer stan

      - name: Test suite (optional)
        if: ${{ steps.detect-scripts.outputs.TEST == '1' }}
        run: php bin/phpunit --no-coverage
