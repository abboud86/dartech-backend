monolog:
  handlers:
    app_json:
      type: stream
      path: '%kernel.logs_dir%/dev.json.log'
      level: debug
      formatter: 'monolog.formatter.json'

    console:
      type: console
      process_psr_3_messages: false
      channels: [ "!event", "!doctrine" ]
