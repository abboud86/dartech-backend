framework:
  rate_limiter:
    auth_refresh:
      policy: 'token_bucket'
      limit: 30
      rate: { interval: '1 minute', amount: 30 }

    auth_logout:
      policy: 'token_bucket'
      limit: 10
      rate: { interval: '1 minute', amount: 10 }
