parameters:
  app.version: '%env(string:APP_GIT_HASH)%'

services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: false

  # Enregistre tout le code App\ comme services (sauf exclusions)
  App\:
    resource: '../src/'
    exclude:
      - '../src/DependencyInjection/'
      - '../src/Entity/'
      - '../src/Kernel.php'

  # Controllers: doivent être PUBLICS + tag 'controller.service_arguments'
  App\Controller\:
    resource: '../src/Controller/'
    public: true
    tags: ['controller.service_arguments']

  # Injection explicite pour VersionController
  App\Controller\Internal\VersionController:
    arguments:
      $appVersion: '%app.version%'
      $envName: '%kernel.environment%'
